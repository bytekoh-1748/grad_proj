Original prompt: 지금 있는 작업을 사진같은 작업으로 다시 만드려고 해. (팝아트 턴테이블 레퍼런스 이미지 첨부)

Context: 카트리지 콘솔(CRAFT BOY)을 버리고, 첨부한 팝아트 턴테이블 그림과
같은 화면을 다시 만든다. LP 를 고르고, 턴테이블 밑의 버튼으로 재생하고,
톤암이 안팎으로 오가고, 재생 5초 뒤에 곡의 인터랙션으로 넘어간다.

- 색판을 노랑·보라·청록·검정·흰색 다섯으로 줄이고 검은 윤곽만으로 깊이를 만들었다.
- 턴테이블을 똑바로 그린 뒤 24도 기울여 왼쪽 위에서 잘리게 놓았다.
  플래터·톤암 축·피치 페이더·거치대의 자리를 톤암의 부채꼴(87.8도~145도)
  바깥으로 배치해, 팔이 어떤 각도에서도 조작부를 지나가지 않는다.
- 판은 SVG 원반 하나가 상자·무대·플래터를 그대로 건너간다. 톤암만 판보다
  위층(.deck-top)에 두어 바늘이 홈에 얹힌다.
- 상자는 CSS 가 정한 두 자리표(anchor-crate-head/tail)가 그리는 대각선 위에
  놓인다. 스크립트는 재기만 하므로 구도는 CSS 에서 바뀐다.
- 고른 판은 활을 그리며 무대 한가운데로 들어오고, 그 박자에 맞춰 제목과
  아티스트가 비스듬히 밀려 들어온다. PLAY 를 누르면 플래터로 옮겨 앉고
  톤암이 들어와 바늘을 내린다. 5초를 세고 나면 곡의 화면으로 넘어간다.
- 여섯 곡을 새로 썼다. 각 파일이 판의 겉면과 인터랙션을 함께 소유한다.
- 확인한 것: 큐 → 재생 → 화면 전환 → 정지 → 상자 복귀의 한 바퀴, 연속 NEXT,
  판 클릭으로 큐(실제 마우스 좌표로도), 일시정지가 톤암을 그 자리에서 들어 올리는지,
  여섯 레이블 그림, 세로 화면 구도, 콘솔 오류 없음.
- 회전한 SVG 원의 getBoundingClientRect 는 √2 만큼 부풀어 나온다. 플래터
  자리는 크기 0인 점(.platter-mark)에서, 크기는 데크의 레이아웃 너비에서 잰다.
  판의 현재 자리도 상자가 아니라 변형 행렬에서 읽는다.
- TODO: 소리는 아직 없다. 물리적인 박자를 눈으로 먼저 확인하려고 뺐다.

2026-09-05 — LP별 팝아트 컬러와 느린 전환
- 사용자 수정 요청에 따라 그라데이션, 파스텔, 블러와 종이 질감을 제거했다.
- 첫 LP는 원래의 노랑·보라·청록을 복원했다. 다른 LP도 라임·핫핑크·코발트,
  청록·코랄·블루, 오렌지·빨강·보라 등 선명한 단색 팔레트를 사용한다.
  모든 판의 윤곽은 검정, 종이는 흰색이며 레이블과 재생 장면은 같은 색을 쓴다.
- 색은 단색 면으로 유지하면서 LP 변경 시 약 3초 동안 천천히 바뀐다.
  재생은 바늘이 앉는 순간부터 7.2초 동안 디졸브, 복귀는 2.6초 동안 이어진다.
- 전환 중 일시정지·재개·선택 변경과 빠른 연속 선택을 지원한다.
  나가는 장면은 원래 LP를 유지하고, 리사이즈 시 오래된 비행 좌표를 취소한다.
- 공통 박자, 별, 꽃, 번개, 포인터 움직임의 갑작스러운 변화를 줄였다.
- 소리는 기존과 같이 구현되어 있지 않다.

2026-09-06 — SVG 벡터 변형으로 방향 수정
- 사용자가 full 3D보다 이전 2D 팝아트 느낌을 선호한다고 수정 요청했다.
- Three.js와 3D 룸 모듈을 제거하고 SVG 원본 도안을 사용하는 구조로 바꿨다.
  진입 파일에 남아 있던 충돌 표시도 새 진입점으로 정리했다.
- 턴테이블은 원본 곡선의 점을 네 모서리에 맞춰 재투영하며 SVG d를 갱신한다.
  방 경계선, 창문 빛, LP 타원, 제목과 버튼도 같은 구도 상태에서 함께 변형된다.
- 감상/펼치기/턴테이블/벽 구도를 감쇠 스프링으로 연결한다. 중간 재조작 시
  현재 좌표와 속도에서 이어져 움직이며 배경 드래그로 선과 면을 조정한다.
- 기존 여섯 곡의 그리기 함수를 SVGContext에 연결해 장면까지 SVG 경로로 출력한다.
  LP별 도안과 팔레트, 포인터 반응, 재생·일시정지·정지 동작은 유지한다.
- 자동 테스트: 모든 구도 사이 뒤집힘·특이점, 전환 중 재조작, 프레임 간격 독립성,
  여섯 곡의 경로 출력·클리핑·좌표 역변환. 실제 음원은 여전히 미연결이다.

2026-09-06 — 바닥·벽·오브젝트의 투영 통합
- 사용자 빨간 가이드처럼 오른쪽으로 내려가는 벽·바닥 경계를 기본 구도로 삼았다.
  실제 화면은 빨간 선 대신 밀도가 옅어지는 도트 띠와 얇은 해칭으로 접점을 표현한다.
- 개별 화면 좌표를 없애고 floor-scene.js의 공통 바닥 좌표에 턴테이블, LP 스택,
  선택한 LP와 제목을 배치했다. 모든 구도·드래그·화면 비율에서 같은 투영을 사용한다.
- 빛은 왼쪽 아래에서 들어와 바닥/벽 접점에서 이어진다. 제목은 짙은 그림자로
  시간에 따라 움직이고 재생 버튼은 그 아래에 놓인다. 07–19시 수동 슬라이더와
  장면 속 자동 시간 흐름을 추가했다.
- 사선/넓게/반대편/벽 쪽의 네 공통 투영으로 전환한다. LP 이동도 바닥 좌표에서
  계산하며 원본 SVG와 팝아트 컬러를 유지한다.
- 자동 검사: 참고 선의 방향, 모든 구도 사이 및 드래그·화면 비율별 평면 뒤집힘,
  벽·바닥 접점, LP 접지, SVG/CSS 투영 일치, 빛의 접점, 전환 연속성과 여섯 SVG 장면.

2026-09-06 — 바닥 홈과 물체의 두께
- 기존 공통 바닥 구도를 유지하고 상판·몸통·판·톤암의 SVG 투영 높이를 나눴다.
  턴테이블 몸통 옆면, LP 단면, 톤암의 그림자, 시간에 따른 바닥 그림자를 추가했다.
- 홈을 음악·영화·신문 포스터 세 장으로 구성했다. Material 3 Expressive의 크기·형태
  대비와 모서리 반응을 참고했다. 포스터를 선택하면 바닥에서 물건들이 순차적으로
  펼쳐져 착지하며, 홈으로 돌아가면 현재 상태에서 반대로 정리된다.
- 영화는 자체 SVG 모션 스터디와 로컬 영상 재생, 신문은 자체 샘플 기사 세 편과 읽기 창을
  연결했다. 실제 음악 파일과 실시간 뉴스 피드는 연결하지 않았다.
- 활동 전환 시 음악의 대기 중 동작을 취소하고 영상도 정지한다. 숨은 활동의 입력과
  포커스를 막고 홈 복귀 초점을 복원한다. 모션 감소 설정은 전환과 자동 움직임을 생략한다.
- 테스트 추가: 높이와 접지의 일치, 그림자 방향, 전환 중 왕복·재선택, 순차 착지와 모션 감소.

2026-09-06 — ROOM / Studies: 네 에디션과 열두 팔레트
- Expressive, Liquid, Bauhaus, Gallery를 같은 바닥 투영 위에 구성했다. 홈 세 포스터의
  각도를 통일하고 형태·타이포·SVG 그래픽·배경 그래픽·물체 재질을 에디션별로 연결했다.
- 하단 에디션·팔레트 선택, 9초 자동 순환, 별도 공간 패널, URL과 브라우저 설정 저장을 추가했다.
  스타일 선택은 현재 활동·음악 재생·LP 위치·구도를 변경하지 않는다.
- Liquid에 반사·반투명 표면과 ‘투명도 줄이기’, 고대비/블러 미지원 대체 표면을 추가했다.
  모션 감소 설정에서는 자동 순환을 제공하지 않고 색과 구도를 즉시 전환한다.
- 원근으로 일부가 잘린 요소에 초점이 갈 때 방 전체가 내부 스크롤되는 문제를 overflow:clip으로 수정했다.
- 테스트 15개 및 프로덕션 빌드 통과. 새 검사는 12개 팔레트의 색상 토큰 대비, 설정 복원,
  전환 중 색상 변경과 프레임 간격 독립성, 포스터의 공통 축·겹침을 다룬다.

2026-09-06 — 실제 시각·LP에 따른 방의 분위기와 바닥 디테일
- Gallery를 제거하고 세 스타일·아홉 팔레트로 정리했다. 홈은 기기의 현재 시각,
  음악은 LP별 아트 디렉션과 시간대를 함께 사용하며 영화·신문도 같은 규칙에 연결했다.
- 프레임 기반 가상 시간 흐름을 없애고 실제 Date를 읽어 창문 빛과 시간대 분위기를 맞췄다.
  밤에는 햇빛이 꺼지고 색·재질은 현재 상태에서 천천히 이어서 전환된다.
- 일반 화면의 스타일 선택 패널을 제거했다. DEV + 루프백 + debug=1에서만 동적으로
  디버그 모듈을 가져오며 배포 빌드에서는 해당 모듈을 제외한다. 실제 preview 서버에서도
  debug·edition·hour URL이 무시되고 실제 시각을 쓰는 것을 확인했다.
- 공통 바닥 패널·이음선·테두리·인레이와 스타일별 도트·곡선·체크·반사선을 추가했다.
  도트까지 바닥 좌표로 투영하고 투영이 바뀔 때만 장식 경로를 다시 계산한다.
- 자동 검사 19개 통과: 시간 경계·LP 매핑·디버그 조건·장식 투영 범위를 추가했다.
- 브라우저에서 실제 17시 경계를 지나며 Sun Machine이 재생 중 Expressive Citrus에서
  Bauhaus Vermilion으로 바뀌는 것을 확인했다. 재생 상태·LP 선택은 유지되었고 콘솔 오류는 없었다.
